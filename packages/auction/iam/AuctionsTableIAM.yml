AuctionsTableIAM:
  Effect: Allow
  Action: # define the actions to be allowed
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:UpdateItem
  Resource:
    - ${self:custom.AuctionsTable.arn}
